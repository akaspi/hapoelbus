<rt-require dependency="../accountData/accountData" as="accountData"/>

<div>
    <div rt-if="this.state.isFetchingData">Loading....</div>
    <div rt-if="!this.state.isFetchingData && !this.hasAccount()" class="create-account">
        <accountData />
    </div>
    <div rt-if="!this.state.isFetchingData && this.hasAccount()" class="dashboard">Dashboard!!!!!!! :)
        <button onClick="{this.logout}">Logout</button>
    </div>
</div>