define(['lodash', 'api/store'], function(_, store) {

  return {
    createUser: _.noop,
    login: _.noop,
    // loginWithFacebook: _.noop,
    // loginWithGoogle: _.noop,
    logut: _.noop,
    isAdmin: _.noop,
  };
});
