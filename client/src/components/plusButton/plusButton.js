'use strict';

var React = require('react');
var template = require('./plusButton.rt');

var PlusButton = React.createClass({
    displayName: 'PlusButton',
    render: template
});

module.exports = PlusButton;
