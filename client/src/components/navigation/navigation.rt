<rt-require dependency="material-ui" as="mui"/>

<div class="navigation">
    <i class="fa fa-bars menu-icon" onClick="{this.toggleNav}"/>

    <mui.LeftNav
            ref="nav"
            docked="{false}"
            menuItems="{this.getNavItems()}"
            onChange="{this.onNavChange}"
            value="{this.state.selectedTab}"/>
</div>